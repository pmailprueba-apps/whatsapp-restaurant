# Avances y Estado del Proyecto — Restaurante Viky Bot WhatsApp (30 Jul 2026)

## 📌 Resumen de Cambios Aplicados en la Sesión

### 1. Prompt y Reglas del Bot (Restaurante Viky)
- **Menú Oficial Inyectado**: Se sincronizó todo el catálogo de `app/menu.py` (Hamburguesas, Sincronizadas, Hotdogs, Tacos, Gringas, Tortas y sus precios).
- **Regla Solo Recoger**: Se estableció explícitamente que NO hay servicio a domicilio (solo para pasar a recoger al local).
- **Sin Nombres ni Dirección**: Se eliminaron presentaciones con el nombre "Viky" y mención de direcciones físicas a menos que el usuario pregunte.
- **Español Obligatorio**: Regla prioritaria #0 para responder únicamente en Español (México).

### 2. Infraestructura y Servidor VPS (`204.168.235.137`)
- **n8n Workflow `pPUfibGXC8vrtbxx`**:
  - `responseMode: "onReceived"` para responder HTTP 200 inmediatamente a los webhooks de WAHA.
  - Ajustes de `settings` en la base de datos de n8n para desactivar el guardado de ejecuciones exitosas (`saveDataSuccessExecution: "none"`) y evitar la saturación de disco.
- **WAHA (OpenWA)**:
  - `retryCount = 0` en la tabla de webhooks de `openwa.sqlite`.
  - URL de webhook apuntando a `https://restauranteviky.duckdns.org/webhook/whatsapp-entrada` (evitando bloqueos SSRF).
- **Microservicio de Deduplicación Persistente (`/root/dedup_server.js`)**:
  - Servidor Node.js independiente corriendo en VPS puerto 3005 (`nohup node /root/dedup_server.js &`).
  - Mantiene en memoria física los IDs de mensajes procesados y una ventana de 8 segundos por `chatId`.
  - Conectado a n8n vía `helpers.httpRequest` al puerto 3005 para validar si un mensaje debe procesarse o ignorarse.

---

## 🔍 Diagnóstico Pendiente para Mañana
- Si persisten respuestas dobles en WhatsApp, revisar:
  1. Si existen instancias adicionales del bot en otros contenedores o servicios (ej. `fabrica-bots-prod-bot-service-1` o PM2 en otro servidor).
  2. Si el proveedor de WhatsApp está duplicando eventos a nivel de protocolo Baileys/WhatsApp Web.
  3. Si se requiere desacoplar por completo n8n y procesar mediante la máquina de estados en NestJS de `39-openwa/fabrica-bots`.
